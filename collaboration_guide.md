# 협업 개발 가이드 (Collaboration & Integration Guide)

이 문서는 '지역 축제 특산품 맞춤형 AI 큐레이터 및 실시간 대기열 서비스'의 **현재 백엔드 개발 현황**과 이를 바탕으로 **AI(ennoia) 파트** 및 **프론트엔드 파트**에서 구현하고 연동해야 할 업무 목록을 정리한 가이드라인입니다.

---

## 1. 현재 백엔드(Spring Boot) 개발 완료 현황
백엔드는 H2 메모리 DB 기반으로 동작하도록 설계되어 있으며, 팀원들이 즉시 테스트할 수 있는 프론트 Mockup UI(`index.html`, `booth.html`)를 함께 제공하고 있습니다.

### ① 축제 추천 및 관광지 혼잡도 수집 (`FestivalController`)
*   **전체 축제 캐싱**: 서버 실행 시 올해(2026년) 전국 대표 16개 축제 데이터가 로컬 DB에 자동으로 캐싱됩니다. (`GET /api/festivals`)
*   **기간 제한 없는 축제 추천**: 거리 조건 범위 내의 모든 축제를 즉시 가져오며, 사용자 피드백을 반영하여 날짜(기간) 제한 필터를 해제했습니다. (`GET /api/festivals/recommend`)
*   **관광지 실시간 혼잡도 연동**: 축제 반경 3km 내 연계 관광지 목록을 획득하고, KT 빅데이터 예측 OpenAPI를 연동해 각 관광지의 혼잡 지수(쾌적, 보통, 혼잡)를 자동으로 수집·매핑합니다.

### ② 실시간 현장 대기열 및 알림 인프라 (`WaitingController`)
*   **비관적 락(Pessimistic Lock) 적용**: 대기 번호표 발급 시 동시성 이슈를 예방하여 여러 명이 동시에 신청해도 순서 번호의 유실이나 중복이 없습니다.
*   **Solapi 실서버 SMS 연동**:
    *   **대기 등록 완료**: 대기 번호 및 대기 팀 수 발송
    *   **고객 호출**: 상인이 대기자를 부를 때 문자 발송
    *   **3순번 이내 자동 임박 알림**: 내 순서가 3순번 이내(앞에 남은 대기 팀이 2팀 이하)가 되면, 다른 대기자가 처리될 때 백엔드 비동기 스레드에서 **자동으로 대상자를 판별해 임박 문자를 발송**합니다.
*   **발신 번호 실시간 변경**: 웹 대시보드 화면에서 테스트할 본인의 발신인 휴대폰 번호를 입력하면 서버 재기동 없이 즉시 Solapi 전송 대상 번호로 변경됩니다.

### ③ QR 코드 발급 및 모바일 랜딩 (`booth.html`)
*   상점별 고유 부스 ID로 QR 코드가 자동 발급됩니다. 모바일 기기로 QR 스캔 시 `booth.html?boothId=1`로 접속되어 **메뉴판 확인 ➡️ 모바일 대기 접수 ➡️ 실시간 내 대기 상태 조회**가 한 흐름으로 이어집니다.

---

## 2. AI (ennoia 큐레이터) 파트 해야할 일

AI 파트에서는 ennoia AI 큐레이터를 활용해 자연어 질의를 파싱하고, 백엔드의 대기 데이터와 추천 데이터를 결합하여 사용자 맞춤형 큐레이션을 제공해야 합니다.

### 💡 주요 핵심 태스크
1.  **자연어 쿼리 분석 및 정교화 (NLP)**:
    *   사용자의 요구사항(예: *"사람 적은 쾌적한 부스 가고 싶어"*, *"맛있는 특산품 메뉴가 있는 부스 위주로 보여줘"*, *"2시간 정도 짧게 돌아볼 만한 축제 코스 추천해줘"*)을 분석해야 합니다.
    *   분석된 텍스트에서 **1) 위치 정보(지명/주소)**, **2) 가용 시간(Hours)**, **3) 선호 성향(혼잡 기피 / 특산품 위주 / 최단 거리 등)** 키워드를 도출합니다.
2.  **AI 큐레이션 전용 API (`/api/ai/curate`) 통합**:
    *   백엔드 AiCurationController.java는 자연어 큐레이션용 API를 열어두었습니다.
    *   이 컨트롤러에 ennoia AI 엔진의 응답 로직을 연계하여, AI 추천 사유(Reason)와 추천하는 축제/부스/관광지 리스트를 결합하여 사용자에게 정제된 자연어로 피드백을 전달해야 합니다.
3.  **혼잡 기피 및 대기 연계 알고리즘 구현**:
    *   부스별 실시간 대기열 수치(`currentWaitingCount`)와 연계 관광지의 혼잡도 예측 지수(`congestionRate`)를 실시간으로 결합하여 *"지금 용문사 주변은 혼잡하니 상대적으로 한적한 들꽃수목원 근처 산나물 부스로 이동하시는 것을 권장합니다"*와 같은 실시간 큐레이션을 텍스트로 생성해 주어야 합니다.

---

## 3. 프론트엔드 (Frontend) 파트 해야할 일

현재 대시보드는 백엔드 기능 테스트 위주로 레이아웃이 잡혀 있으므로, 프론트엔드 파트에서는 프리미엄 디자인(다크모드, 글래스모피즘 등)으로 고도화하고 모바일 사용자 경험(UX)을 극대화해야 합니다.

### 💡 주요 핵심 태스크
1.  **모바일 대기 접수 뷰 (`booth.html`) 고도화**:
    *   QR 코드를 찍고 들어오는 모바일 사용자가 이용할 화면입니다.
    *   현재 화면은 4초 주기 폴링(Polling)으로 내 순번을 추적하고 있습니다. 이를 모바일 웹앱 형태로 매끄럽고 고급스러운 컴포넌트 디자인으로 변환해 주세요.
    *   메뉴판에서 해당 축제의 **지역 특산품(IsSpecialty=true)** 표시가 된 메뉴가 강조되어 노출되도록 렌더링을 강화해 주세요.
2.  **카카오맵 API 지도 컴포넌트 활성화**:
    *   현재 백엔드에서 제공하는 `/api/config/kakao-key` API를 호출하여 프론트엔드에서 카카오 지도의 Javascript SDK를 동적으로 초기화하는 코드를 구현해야 합니다.
    *   추천된 축제 위치와 그 주변의 관광지(들꽃수목원, 용문사 등)의 위경도 좌표를 활용해 지도상에 핀(Marker)을 꼽고, 각각의 혼잡 지수(쾌적: 녹색, 보통: 황색, 혼잡: 적색 등)를 시각적으로 맵 마커에 반영해 주세요.
3.  **상인 전용 관리 대시보드 UI 구현**:
    *   상인이 자신의 점포를 등록하고(`POST /api/booths`), 대기 팀들을 한눈에 보며 **[호출]**, **[방문 완료]**, **[취소]** 버튼을 누를 수 있는 직관적인 상인용 전용 인터페이스 태블릿/PC 뷰를 다듬어 주세요.
    *   고객 호출 시 백엔드의 `/api/waitings/{waitingId}/call`을 정상 호출하여 고객에게 Solapi를 통한 호출 카카오톡/알림 문자가 즉각 가도록 버튼 이벤트를 연결해야 합니다.

---

## 4. API 연동 정보 요약 (개발 참고용)

| API 엔드포인트 | Method | 설명 | 주요 파라미터 |
| :--- | :---: | :--- | :--- |
| `/api/festivals` | `GET` | 캐싱된 전체 축제 목록 조회 | 없음 |
| `/api/festivals/recommend` | `GET` | 거리 기준 맞춤형 축제 및 관광지 추천 | `address`, `maxDistanceKm`, `latitude`, `longitude` |
| `/api/booths` | `POST` | 상인 신규 부스 및 메뉴판 등록 | `festivalId`, `name`, `products` (리스트) |
| `/api/festivals/{festivalId}/booths` | `GET` | 특정 축제의 전체 부스 대기열 목록 조회 | `festivalId` (Path) |
| `/api/waitings` | `POST` | 모바일 대기열 신규 신청 (QR 스캔 연계) | `boothId`, `phoneNumber` |
| `/api/waitings/{waitingId}/call` | `POST` | 대기 번호 호출 (상인용 - 알림 발송 트리거) | `waitingId` (Path) |
| `/api/spots/congestion` | `GET` | 관광지 집중률(혼잡도) 예측 API 단독 진단 | `spotName` (Query) |

---
**문의 사항**: 백엔드 소스코드 내 [ApplicationReadyEvent] 핸들러와 서비스 계층에 세부 연동 코드가 주석과 로그로 친절하게 구성되어 있으므로, 깃허브 코드를 clone 받아 `FestivalService.java` 및 `WaitingService.java`를 참고해 주시기 바랍니다.
